This paragraph is rendered from an actual `.mdx` file, imported into the digest
page — confirming the MDX pipeline (`@next/mdx` + `mdx-components.tsx`) is
wired correctly. Real digest content replaces this in Bite 2.
